extends Spatial

export var health = 0
export var state = "" #can be "placing", "rotating" or "placed"

func _ready():
	# Called every time the node is added to the scene.
	# Initialization here
	pass
